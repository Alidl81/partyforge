# اعمال نسخهٔ اتصال سریع LAN

این بسته فرم فنی شش‌فیلدی را با پیدا کردن خودکار اتاق‌های نزدیک جایگزین می‌کند.

## روش اعمال Patch

محتویات فایل Patch را مستقیماً در ریشهٔ Repository استخراج و فایل‌های موجود را جایگزین کنید؛ همان پوشه‌ای که `pubspec.yaml` در آن قرار دارد.

سپس:

```powershell
git add .
git commit -m "Add automatic LAN room discovery and one-tap join"
git push
```

در GitHub یک اجرای جدید بسازید:

```text
Actions → Build PartyForge → Run workflow
```

از Re-run اجرای قدیمی استفاده نکنید، چون Commit قبلی را دوباره اجرا می‌کند.

## تجربهٔ جدید

1. میزبان «ساخت اتاق» را می‌زند.
2. بازیکن دیگر «پیوستن به اتاق» را می‌زند.
3. اتاق‌های روی همان Wi-Fi یا هات‌اسپات خودکار نمایش داده می‌شوند.
4. بازیکن روی «پیوستن» می‌زند؛ نیازی به IP، Port، Session ID یا Token نیست.
5. کد شش‌رقمی و IP فقط داخل بخش پیشرفته و برای شرایط خاص باقی مانده‌اند.

## نکتهٔ Windows

در اولین اجرای Host ممکن است Windows Firewall سؤال بپرسد. دسترسی را برای **Private networks** مجاز کنید. Public networks لازم نیست فعال شوند.
